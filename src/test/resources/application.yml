app:
  datasource:
    jdbc-url: jdbc:h2:mem:testdb
    username: sa
    password: password
  jpa:
    hibernate.ddl-auto: update
    generate-ddl: false
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect